"use client";
import { Canvas } from "@react-three/fiber";
import { OrbitControls, Stage, PerspectiveCamera, Environment } from "@react-three/drei";
import { Suspense } from "react";

export default function STLViewer() {
  return (
    <div className="w-full h-full cursor-grab active:cursor-grabbing">
      <Canvas shadows>
        <Suspense fallback={null}>
          <PerspectiveCamera makeDefault position={[100, 100, 100]} fov={50} />
          <Stage adjustCamera intensity={0.5} environment="city">
            {/* Modelo Placeholder (Cubo Industrial) */}
            <mesh rotation={[0, Math.PI / 4, 0]}>
              <boxGeometry args={[40, 40, 40]} />
              <meshStandardMaterial 
                color="#222" 
                roughness={0.1} 
                metalness={0.8}
                wireframe={false} 
              />
              {/* Efecto de borde neón */}
              <mesh>
                <boxGeometry args={[40.5, 40.5, 40.5]} />
                <meshBasicMaterial color="#00F2FF" wireframe />
              </mesh>
            </mesh>
          </Stage>
          <OrbitControls makeDefault autoRotate autoRotateSpeed={0.5} />
          <Environment preset="night" />
        </Suspense>
      </Canvas>
    </div>
  );
}