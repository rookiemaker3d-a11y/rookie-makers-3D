{
  "name": "rokie",
  "private": true,
  "scripts": {
    "start": "concurrently -n fe,be \"npm run dev --prefix frontend\" \"cd backend && venv\\Scripts\\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001\"",
    "seed": "cd backend && venv\\Scripts\\python.exe -m app.seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
