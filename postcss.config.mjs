/** @type {import('postcss-load-config').Config} */
// Tailwind v4 se procesa vía @tailwindcss/vite, no necesita estar aquí.
const config = {
  plugins: {
    autoprefixer: {},
  },
};

export default config;
